/*
    @file assert.h
    @brief Система утверждений для отладочной проверки условий.
    @author Дмитрий Скляр.
    @version 1.2
    @date 25-02-2026

    @license Лицензия Apache, версия 2.0 («Лицензия»);
          Вы не имеете права использовать этот файл без соблюдения условий Лицензии.
          Копию Лицензии можно получить по адресу http://www.apache.org/licenses/LICENSE-2.0
          Если иное не предусмотрено действующим законодательством или не согласовано в письменной форме,
          программное обеспечение, распространяемое по Лицензии, распространяется на условиях «КАК ЕСТЬ»,
          БЕЗ КАКИХ-ЛИБО ГАРАНТИЙ ИЛИ УСЛОВИЙ, явных или подразумеваемых. См. Лицензию для получения
          информации о конкретных языках, регулирующих разрешения и ограничения по Лицензии.

    @note Предоставляет:
            - Макрос ASSERT для проверки условий в отладочной сборке
            - Автоматическое отключение проверок в релизной сборке
            - Детализированные сообщения об ошибках с указанием файла и строки
            - Остановку выполнения в отладчике при срабатывании утверждения
            - Безопасную конструкцию do-while для избежания проблем с операторами
*/

#pragma once

#include <core/defines.h>

#ifdef DEBUG_FLAG
    /**
        @brief Проверяет утверждение с аварийной остановкой программы при несоответствии в отладочной сборке.
        @param expr Проверяемое выражение (должно быть true в нормальной ситуации).
        @param file Имя исходного файла, где обнаружено ложное утверждение (обычно __FILE__ или __FILE_NAME__).
        @param line Номер строки исходного файла, где обнаружено ложное утверждение (обычно __LINE__).
        @param func Функция в исходном файле, где обнаружено ложное утверждение (обычно __func__ или __PRETTY_FUNCTION__).
        @param format Строка или указатель на строку форматируемого сообшения для вывода на консоль.
        @param ... Список аргументов форматируемого сообщения.
    */
    CORE_API void assert(const char* expr, const char* file, u32 line, const char* func, const char* format, ...);

    /**
        @brief Проверяет утверждение с аварийной остановкой программы при несоответствии в отладочной сборке.
        @param expr Проверяемое выражение (должно быть true в нормальной ситуации).
        @param ... Строка или указатель на форматируемую строку сообщения с или без аргументов для вывода на консоль.

        @note Если выражение ложно: выводит сообщение об ошибке с текстом условия, сообщением,
        файлом, строкой и аварийно останавливает выполнение программы. В отладчике перед аварийным
        завершением программы производит остановку выполнения программы.
    */
    #define ASSERT(expr, ...)                                                                          \
        do {                                                                                           \
            if(UNLIKELY(!(expr))) assert(#expr, __FILE__, __LINE__, __PRETTY_FUNCTION__, __VA_ARGS__); \
        } while(false)
#else
    /**
        @brief Проверяет утверждение с аварийной остановкой программы при несоответствии в отладочной сборке.
        @param expr Проверяемое выражение (должно быть true в нормальной ситуации).
        @param ... Строка или указатель на форматируемую строку сообщения с или без аргументов для вывода на консоль.

        @note Если выражение ложно: выводит сообщение об ошибке с текстом условия, сообщением,
        файлом, строкой и аварийно останавливает выполнение программы. В отладчике перед аварийным
        завершением программы производит остановку выполнения программы.
    */
    #define ASSERT(expr, ...) UNUSED(0)
#endif
